import { Fragment } from 'react';
import { Header, Footer } from '../';

const Wrapper = ({ children }) => {
  return (
    <div className={}>
      {children}
    </div>
  );
};

export { Wrapper };
