import React from 'react'
import styles from './footer.module.scss';

const Footer = ({ }) => {
  return (
    <footer className={styles.footer}>
      Petfinder - відкритий студенський проект
    </footer>
  )
}

export { Footer }