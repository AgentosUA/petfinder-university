name: Autodeploy dev
on: 
  push:
    branches: [ dev ]
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ssh {{ secrets.SSH_USER }}@{{ SSH_IP }} -p {{ SSH_PORT }}
      - run: mkdir test
